/**
 * ScrollTo function parameter type
 */
export interface ScrollToOptions {
	/** If we align the element at the end or at the start of the screen */
	align: 'start' | 'end';
	/** The margin from the screen border in percentage */
	margin: number;
}
